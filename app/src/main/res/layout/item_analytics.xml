<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="50dp">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/iv_icon_analytics"
        android:layout_width="50dp"
        android:layout_height="match_parent"
        android:padding="12dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:srcCompat="@drawable/category_icon_business" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_title_analytics"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        tools:text="Магазин"
        android:fontFamily="@font/roboto_regular"
        android:textColor="@color/dark_text"
        android:textSize="16sp"
        android:layout_marginStart="12dp"
        app:layout_constraintEnd_toStartOf="@+id/tv_amount_analytics"
        app:layout_constraintStart_toEndOf="@+id/iv_icon_analytics"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_amount_analytics"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        tools:text="1000"
        android:fontFamily="@font/roboto_medium"
        android:textColor="@color/dark_blue"
        android:textSize="16sp"
        android:gravity="center_vertical"
        android:layout_marginEnd="20dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>